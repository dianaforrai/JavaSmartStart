# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/glologistics_db
spring.datasource.username=postgres
spring.datasource.password=Hugo2002.
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# (Optional) H2 console disabled since we're using Postgres
spring.h2.console.enabled=false

# JWT Configuration
jwt.secret=mySecretKey
jwt.expiration=86400000

# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=diana.forrai98@gmail.com
spring.mail.password=Hugo2002.
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Application Configuration
app.name=Hitachi Mobile SIM Portal
server.port=8080

